<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.firstProject.mapper.recipe-mapper">
 <select id="recipeData" resultType="com.firstProject.vo.RecipeVO" parameterType="hashmap">
    SELECT recipe_no, poster, title, mem_id, num
    FROM (SELECT recipe_no, poster, title, mem_id, rownum as num
    FROM (SELECT recipe_no, poster, title, mem_id
    FROM recipe))
    WHERE num BETWEEN #{start} AND #{end}
  </select>
  <select id="recipeTotalPage" resultType="int" parameterType="int">
    SELECT CEIL(COUNT(*)/12.0) FROM recipe
  </select>
   <update id="hitIncrement" parameterType="int">
    UPDATE recipe SET
    hit=hit+1
    WHERE recipe_no=#{recipe_no}
  </update>
  <select id="recipeDetailData" resultType="com.firstProject.vo.RecipeVO" parameterType="int">
    SELECT * FROM recipe
    WHERE recipe_no=#{recipe_no}
  </select>
  
  
  <!-- 찜 저장  -->
  <insert id="likeInsert" parameterType="com.firstProject.vo.LikeVO">
    INSERT INTO like_recipe VALUES(
      (SELECT NVL(MAX(recipe_no)+1,1) FROM like_recipe),#{mem_id},#{cno}
    )
  </insert>
  <!-- 데이터 읽기(찜 목록) -->
  <select id="likeListData" parameterType="String" resultType="com.firstProject.vo.LikeVO">
    SELECT * FROM like_recipe
    WHERE mem_id=#{mem_id}
    ORDER BY recipe_no DESC
  </select>
  <!-- 찜여부 확인 -->
  <select id="likeCount" parameterType="com.firstProject.vo.LikeVO" resultType="int">
    SELECT COUNT(*) FROM like_recipe
    WHERE mem_id=#{mem_id} AND cno=#{cno}
  </select>
  <!-- 찜 취소시에 처리  -->
  <delete id="likeDelete" parameterType="int">
    DELETE FROM like_recipe
    WHERE rest_no=#{rest_no}
  </delete>
</mapper>